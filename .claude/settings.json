{
  "enableAllProjectMcpServers": true,
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--browser",
        "chrome",
        "--headless"
      ]
    }
  },
  "enabledPlugins": {
    "commit-commands@claude-plugins-official": true
  }
}
